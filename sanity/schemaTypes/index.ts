import { clinicConfig } from "./clinicConfig";
import { localeString } from "./localeString";
import { linkItem, localizedStringArray, workingHour } from "./shared";

export const schemaTypes = [localeString, linkItem, workingHour, localizedStringArray, clinicConfig];
